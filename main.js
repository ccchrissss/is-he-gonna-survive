function hero(bullets, dragons){
  return bullets >= dragons * 2 ? true : false
}
  
  
  // p: bullets, dragons    // numbers
  // r: boolean
  // e.g. hero(7, 3)
  // results in true

  // return ternary expression evaluating true if bullets is greater than or equal to dragons * 2